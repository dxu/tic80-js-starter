{
  "scripts": {
    "build": "yarn run bundle ; yarn run uglify",
    "bundle": "node scripts/bundler",
    "uglify": "uglifyjs --compress --mangle --output bundle.min.js -- bundle.js"
  },
  "devDependencies": {
    "module-concat": "^2.3.0",
    "uglify-js": "^3.4.9"
  }
}
